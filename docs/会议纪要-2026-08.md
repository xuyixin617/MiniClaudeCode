# 会议纪要（2026-08）

## 议题：登录鉴权方案选型
结论：放弃服务端 session，改用 JWT 双令牌，理由是无状态、便于水平扩容。
待办：小明负责接入网关统一鉴权；小红下周完成 refresh_token 黑名单。

## 议题：接口限流
结论：网关层加令牌桶限流，默认每 IP 每秒 50 次，可配置。
风险：误伤共享出口 IP 的客户端，需支持白名单。

## 议题：部署回滚演练
结论：本月内在 staging 做一次「发布→发现异常→回滚」演练，验证回滚脚本可用性。
