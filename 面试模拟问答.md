# MiniClaudeCode 面试模拟问答

> 按「后端 / AI Agent 面试官」视角组织，由浅入深 + 连环追问，共 7 大类。
> 用法：先自己口头答，再对照参考答查漏补缺。**别背**，重点是能用自己的话讲清「为什么这么设计」。

---

## ⚠️ 使用前必读：四个最容易穿帮的地方

面试官看到项目介绍，**第一反应是抓数字和术语**。下面四处务必提前统一口径：

1. **「13 个核心工具」——写错了，实际是 16 个**（`tools/defaults.py` 注册 16 个）。要能当场数：7 个常驻 + 9 个延迟激活（见 Q2 追问5）。面试前要么改介绍，要么心里有数。
2. **「16+ 危险命令正则」——实际是 20 个模式**（`permission/dangerous.py`）。说「20 类」更硬气；`manager.py` 注释里写「18+」也和实际不一致，别照着念。
3. **「前 5 轮节省 5K+ Token」** —— 目前没有 benchmark 支撑。要么补测量脚本（全量 schema vs 延迟激活 schema 的 token 差 × 轮数），要么诚实说「对照实验估算值」。
4. **「多工具执行时间降低 15%」** —— 同上，需说明基准（几个工具、哪些工具、跑几次取平均）。

---

## 一、项目基础理解

### Q1：简单介绍下 MiniClaudeCode，解决什么核心痛点？

**参考答**：一个面向编码场景的轻量级本地 Coding Agent CLI，核心解决三个「Agent 长程任务不稳」的痛点：

1. **工具可控调用** —— 模型会乱调/幻觉调工具，用「延迟激活 + 权限预检 + 未知工具兜底」约束；
2. **上下文可持续** —— 长任务对话越来越长，用「4 层渐进式压缩 + 跨会话记忆」维持密度；
3. **权限安全可预期** —— 写文件/执行命令有风险，用「四层权限体系」让每次副作用都可控、可审计。

技术栈刻意保持轻：Python + AsyncIO + httpx（无 LangChain/LangGraph），自研 agent runtime。

> **追问1：什么是 Harness Engineering？为什么这个项目叫「面向 Harness Engineering 的 Coding Agent」？**

**参考答**：Harness 本义是「给马套缰绳」。Harness Engineering 指的是**不满足于「把模型接上工具能跑」，而是构建一套可控的执行框架**——工具系统、上下文管理、权限、重试、观测——让 Agent 在长程任务里稳定、可预期。这个项目的定位就是把 Agent 的这些底层机制自己实现一遍，所以是「面向 Harness Engineering」。

> **追问2：对比 Aider / Claude-Code / Cursor Agent，你的差异化在哪？**

**参考答**：它们是成熟产品，功能全但黑盒、重。我的差异化不在「功能更多」，在「更透明、更轻、更可控」：自研 runtime（协议/重试/并发/压缩每层自己写、可解释）、无框架重依赖、四层权限 + 四层压缩 + 跨会话记忆都可拆开看。定位是「教学 + 轻量落地」，不是对标产品功能面。

---

## 二、Agent 循环 & 工具系统（重点模块）

### Q2：讲一讲 Agent 主循环的整体流程。

**参考答**（一条消息的完整链路）：

```
① 工具延迟激活：按输入命中关键词，决定注入哪些工具 schema
② sideQuery：用输入当 query 召回跨会话记忆
③ 组装多层级 system prompt（基础/项目/记忆/动态）
④ 工具往返循环：
   调模型 → 权限预检 → 并发执行工具（失败回退串行）→ 注入结果
⑤ 每轮按上下文利用率触发软/硬压缩
⑥ 模型返回纯文本 → 结束本轮
```

> **追问1：什么是工具延迟激活？怎么做到节省 5K+ token？工具定义什么时候注入上下文？**

**参考答**：
- **原理**：16 个工具的完整 JSON schema 每轮都发给模型非常占 token。延迟激活 = 每个工具绑一组触发关键词（如 `web_search` 绑「搜索/查一下/search」），只有用户输入命中关键词，才把该工具的 schema 加入本轮发给模型的 tools 列表。
- **常驻工具**（7 个，始终注入）：read_file / write_file / edit_file / glob / grep / bash / todo_write。
- **延迟工具**（9 个，命中关键词才注入）：web_fetch / web_search / task / memory_save / memory_recall / list_memory / rag_index / rag_search / rag_list。
- **5K 口径**：对比「全量 16 工具 schema」vs「延迟激活后 schema」的序列化 token 差，再乘轮数。这是估算值，要说清基准。
- **注入时机**：每轮 `run()` 开头调用 `lazy_activate_by_text(user_text)` 更新激活集合，真正发请求时 `active_schemas()` 才取出。

> **追问2：流式工具执行、工具并发回退，15% 耗时降低怎么实现？并发执行会遇到哪些问题？**

**参考答**：
- **并发**：模型一轮可能返回多个 tool_calls，权限预检通过后，用 asyncio **并发执行**；registry 内部对「不支持并发 / 依赖冲突」的**回退串行**。
- **15% 口径**：并发 vs 串行的耗时对比，需说明基准（几个工具、几次平均）。
- **并发会遇到的问题**：
  1. **多个工具改同一文件** → 靠 read-before-edit + mtime 校验兜底，后写者发现 mtime 变了就拒绝；
  2. **工具间有依赖**（A 的输出是 B 的输入）→ 并发会乱序，需回退串行；
  3. **交互式权限确认会打架** → 所以权限预检是**串行**的（`loop.py` 逐个 `check`），确认通过后才并发执行。

> **追问3：read-before-edit + mtime 新鲜度校验，讲完整流程。mtime 解决什么场景？文件被外部修改怎么处理？**

**参考答**：
- **完整流程**：模型要 edit → 必须先 read_file，把文件内容和 mtime 记录进 `ctx.file_states` → edit 前校验两点：① 是否读过；② 当前文件 mtime 与记录 mtime 差值是否 `> 1e-6`，超了就拒绝。
- **mtime 解决**：TOCTOU 竞态——「读完之后、改之前」文件被外部程序改了，避免用旧内容覆盖新改动。
- **外部改动的处理**：edit 被拒绝，返回「文件自上次读取后已被外部改动，建议重新 read_file」，模型重读后再改。

> **追问4：指数退避 + 抖动重试，抖动为什么不能省略？重试哪些异常，哪些绝对不能重试？**

**参考答**：
- **实现**：`delay = base × 2^attempt × jitter`，jitter ∈ `[0.5, 1.5]`（满抖动）。
- **抖动不能省**：固定重试只能错开「单客户端」，但「多个客户端同时被限流、同时重试」仍会再次撞上（惊群效应 thundering herd）。满抖动让重试时间均匀散开。
- **重试哪些**：可重试状态码 `{408, 429, 500, 502, 503, 504}`。
- **绝不能重试**：401/403 鉴权错误、400 参数错误（重试无意义）；以及任何**已产生副作用**的操作——重试层只管「后端 API 请求本身」，不会重放工具执行。

> **追问5：13（16）个核心工具列举关键几个，工具调用返回结果过大怎么处理？**

**参考答**：
- **列举**：文件 read/write/edit、搜索 glob/grep、shell bash、web fetch/search、元工具 todo_write / task / memory_save / memory_recall / list_memory、RAG rag_index / rag_search / rag_list。
- **返回过大处理**（两级）：
  1. **registry 统一截断**：`DEFAULT_MAX_OUTPUT = 30_000` 字符，超限截断；上下文紧张（利用率 ≥50%）时收紧到 `TIGHT_MAX_OUTPUT = 8_000`；
  2. **bash 单独更严**：`_MAX_OUTPUT = 20_000`，超限加「已截断 N 字符」提示。
  3. 再往上就是 stale_snip（剪掉旧工具结果）+ microcompact（工具结果限 2000 字符）。

---

## 三、CLI 和系统提示词

### Q3：介绍多层级 system prompt 注入架构。

**参考答**：四层，按「通用 → 具体」拼接：
- **① 基础层**：角色 / 工作纪律 / 安全底线（永远在）；
- **② 项目层**：CLAUDE.md 规则（有才拼）；
- **③ 记忆层**：sideQuery 每轮现查的跨会话记忆（有才拼）；
- **④ 动态层**：日期 / 工作目录 / 权限模式 / 模型（永远在）。

好处是改一层不影响其他层；排序语义是「越靠后的层越具体、越该优先遵守」。

> **追问1：CLAUDE.md + @include 递归解析，递归怎么防循环引用？**

**参考答**：维护一个「当前链上已处理文件」的集合，再碰到同一个文件就跳过并留占位（`[循环引用]`）；同时设最大嵌套深度（10）兜底防环。踩过的坑：相对路径最初写成了「相对启动目录」，后来改成「相对被 include 文件自己的目录」，并补了测试才暴露。

> **追问2：要求控制 CLAUDE.md 开销在 200 行以内，如果用户写的 md 远超 200 行，做什么限制策略？**

**参考答**：合并后**保留前 200 行**，超出部分换成一行「已截断」提示。因为「就近的规则排前面」，砍尾部恰好保住的是**最具体、最该遵守**的规则，先牺牲全局兜底。这是开销的硬上限。

> **追问3：slash commands、会话持久化，会话保存哪些内容？哪些不保存？会话恢复怎么重建 Agent 状态？**

**参考答**：
- **保存**：session_id、title、model、mode、完整对话 history（`SessionStore.save`）。
- **不保存**（独立持久化，天然跨会话）：跨会话记忆（`memory.json`）、RAG 索引（`rag.db`）、权限白名单（`whitelist.json`）。
- **重建**：load 读出 history → `restore_history` 塞回 loop；system prompt 每轮重新组装（claude_content 重新 load、memory 重新 side_query）。所以「恢复」= 恢复对话历史 + 元信息，其他状态靠独立文件持久化。

---

## 四、权限安全（四层权限体系）

### Q4：讲清楚四层权限体系，5 种权限模式分别什么含义。

**参考答**：
- **四层**：模式层 → 规则层 → 危险层 → 交互层。
- **判定顺序**（`PermissionManager.check`）：`bypass → deny 规则 → allow 规则 → 白名单 → 危险检测 → 模式/类别默认 → 交互确认`。
- **5 种模式**：default / plan / acceptEdits / bypassPermissions / dontAsk。

> **追问1：5 种模式分别什么时机触发？**

**参考答**：
- `default`：写操作默认交互确认；
- `plan`：只读，拒绝一切副作用操作（规划态）；
- `acceptEdits`：文件编辑类（write/edit/memory_save）自动放行，其余确认；
- `bypassPermissions`：完全信任，跳过所有确认（check 第一步直接放行，连 deny 规则都跳过）；
- `dontAsk`：只读之外一律自动拒绝（只读/演示态）。

> **追问2：allow/deny 声明式规则，规则匹配优先级怎么设计？模式和 allow-deny 冲突谁更高？**

**参考答**：
- **规则优先级**：`deny > allow`——只要有 deny 命中就返回 deny（`evaluate` 里 deny 优先返回）。
- **模式 vs 规则冲突**：**bypass 模式压一切**（直接放行，忽略 deny 规则）；**deny 规则 > allow 规则**；plan / dontAsk 模式是「规则没命中」之后的**默认兜底**。一句话：bypass > deny > allow > 模式默认。

> **追问3：16+（20）高危命令正则检测，正则有什么天然缺陷？如何绕过正则？除正则外还有什么加固手段？**

**参考答**：
- **缺陷**：正则只匹配「字符串形态」，命令可用变量拼接、`$(echo rm)`、base64、换行/引号、别名等方式绕过；且会**误报**（注释里出现 `rm -rf` 也命中）。
- **正则之外的加固**：
  1. **路径越界检测**（`_path_danger`）：写文件路径逃出工作目录直接判高风险——这是**结构化**判断，正则绕不过；
  2. **deny 规则兜底**：正则漏了，管理员可配 deny；
  3. **最终落到交互确认**：正则只是**标记风险**、不直接拒绝，用户最后把关；
  4. 白名单反向约束。

> **追问4：白名单 + 用户确认，自动化效率和安全是矛盾点，怎么平衡？什么场景强制弹窗不能跳过？**

**参考答**：
- **平衡**：分层——只读工具默认放行（效率），写/命令才确认（安全）；acceptEdits 自动接受编辑类；白名单把「确认过的高频操作」沉淀成持久化 allow 规则，下次不打扰。
- **强制确认不能跳过**：**危险命令命中 + 路径越界写入**，即使 acceptEdits 模式也**不会**自动放行（代码里 acceptEdits 放行的条件是 `not danger_reason`），必须走确认。

---

## 五、上下文与记忆（项目难点）

### Q5：详细讲解四层渐进式压缩流水线。

**参考答**（从便宜到贵、从粗到细）：

| 层级 | 做什么 | 成本 |
|---|---|---|
| budget_truncate | FIFO 硬截断，每次必执行的兜底保险丝 | 零（字符串） |
| stale_snip | 剪掉较早的工具返回，留「开头 200 字 + 提示」，最近 6 条完整 | 零（字符串） |
| microcompact | 折叠空白 + 头尾各留一半截断（普通 4000 / 工具 2000 字符） | 零（字符串） |
| auto_compact | 唯一调 LLM 的一层，把老历史提炼成 800 字结构化摘要 | 一次 LLM 调用 |

> **追问1：触发阈值 50%-70% 收紧工具输出、>85% 触发 auto-compact，利用率怎么计算？是 token 计数吗？**

**参考答**：利用率 = 已用 token / 上下文窗口。已用 token 是每轮 `result.usage.total`（后端返回的**真实 usage**）累计。阈值是经验值 + 渐进分级：50% 收紧工具输出体积、70% 软压缩、85% 硬压缩。

> **追问2：四层逐级递进，每层分别做什么？什么条件从 microcompact 升级到 auto-compact？**

**参考答**：
- 每层职责见上表。
- **升级条件**：利用率从 70%（软压缩）涨到 **85% 仍压不下来**，才升级到 auto-compact——因为 auto-compact 要花一次 LLM 调用，只在「便宜的字符串手段不够用」时才值得。

> **追问3：sideQuery 语义召回、异步预取、新鲜度提醒，讲下实现逻辑。为什么需要 sideQuery，不直接把全部历史塞上下文？**

**参考答**：
- **sideQuery**：每轮用用户输入当 query 去记忆库召回相关条目，作为「侧边上下文」注入本轮。
- **为什么不塞全部历史**：历史会超长、会被压缩、会丢，且大部分历史与当前问题无关。sideQuery 是「按需取相关」，不是「全量塞」。
- **异步预取**：启动时后台预热记忆索引，避免首轮召回阻塞。
- **新鲜度提醒**：read_file 时记录 mtime，后续检测文件是否被外部改动，改了提示重读（和 Q2 追问3 呼应）。

> **追问4：跨会话记忆，不依赖对话历史，记忆存什么？什么时候更新？怎么避免记忆污染、过期记忆？**

**参考答**：
- **存什么**：content（内容）、tags（标签）、created_at（时间戳）、embedding（语义向量，可选，save 时缓存）。
- **什么时候更新**：`memory_save` 工具显式保存时。
- **防污染 / 过期**：召回打分 = 语义余弦（或词元重叠）+ 标签命中加分 + 时效加分（越新权重越高），再 limit 截断。**诚实说**：目前没有显式的过期淘汰机制，靠打分排序 + limit 兜底，这是可改进点。

> **追问5：长会话压缩之后会不会丢关键信息，怎么兜底？**

**参考答**：
1. auto_compact 的 `_SUMMARY_PROMPT` **强制保留 5 点**（用户目标 / 关键决策 / 文件改动 / 错误与修复 / 进度待办）；
2. **保留最近 6 条完整**（keep=6），只摘要更早的部分，当前上下文不丢；
3. **摘要失败则保留原文**（`return messages`），宁可上下文长一点也不丢历史；
4. budget_truncate 会**保留 system 层**（项目规则不参与截断）。

---

## 六、工程落地、问题排查

### Q6：整个项目基于 asyncio，说下异步踩过的坑。工具并发执行时，异步会遇到什么风险？

**参考答**：
- **踩坑**：① 中文路径/编码（Windows 中文用户名导致 editable install 的 `.pth` 崩溃）；② 阻塞调用要 `asyncio.to_thread`（读用户输入不能阻塞事件循环）；③ 工具并发改同一文件的竞态。
- **并发风险**：多个 tool_calls 并发，若都改同一文件会互相覆盖——靠 read-before-edit + mtime 兜底；交互式权限确认不能并发（预检串行）。

> **追问1：同时兼容 Anthropic 和 OpenAI 两套 API，工具调用格式不一样，怎么做适配层？**

**参考答**：内部统一用 OpenAI 风格（messages 是 role/content，tools 是 function 风格）。适配层做两件事：
- `_to_anthropic_messages`：把 OpenAI 消息翻译成 Anthropic 的「system 字符串 + messages」——system 抽成顶层字段、tool 结果转 `tool_result` 块、assistant 的 tool_calls 转 `tool_use` 块；
- `_tools_to_anthropic`：function 风格 tools → `input_schema`。
- **踩的坑**：Anthropic 的 system 是顶层字段不是 `role=system`；工具参数字段叫 `input_schema` 不叫 `parameters`；流式的 tool 参数是 `partial_json` 增量拼接。

> **追问2：如果大模型频繁幻觉、乱调用不存在的工具，项目做了哪些防御？**

**参考答**：
1. **registry 兜底**：`if tool is None: return ToolResult.error("未知工具")`——不存在的工具名被拦下返回 error，不崩溃；
2. **权限层**：被拒绝的工具返回「[权限拒绝]」占位，不中断循环；
3. **延迟激活**：不相关工具不进 schema，从源头减少误调用。
4. **诚实说**：目前没有对「幻觉 tool_call」的显式惩罚/纠正机制，这是可改进点。

> **追问3：项目怎么做可观测？如何统计 token 消耗、工具调用成功率、压缩触发频次？**

**参考答**：
- **已做**：token 消耗（`ctx.token_used` 累计 `result.usage.total`）；工具活动（`on_event` 回调展示 tool_call / tool_result）。
- **诚实说短板**：「工具调用成功率」「压缩触发频次」「权限决策分布」目前**没有专门的 metrics 统计**。改进方向：加结构化日志/指标采集（每轮记录 token、工具名、耗时、权限决策、压缩触发）。

---

## 七、开放性压力提问

### Q7：如果让你迭代下一版，优先优化哪个模块？为什么？

**参考答**：优先两个（二选一，都要能说清理由）：
1. **可观测性**——补全 metrics（token / 工具成功率 / 压缩频次 / 权限决策），因为没观测就没法优化其他模块，也支撑得了 Q2 那些「5K/15%」数字；
2. **延迟激活改成每轮回收**——当前是「只增不减」，长会话里工具会累积、还会出现「上轮激活的 web_search 干扰这轮 rag_search」的误调用，改成每轮按相关性动态裁剪。

> **追问：这个项目放到生产环境，最大风险是什么？**

**参考答**：
1. **权限「allow 规则先于危险检测」**——误配宽泛 allow 会放行危险命令；
2. **延迟激活不回收**导致工具累积 + 误调用；
3. **压缩有损**可能丢关键信息；
4. **缺可观测**，出问题难排查；
5. 危险命令正则可被绕过（需靠路径越界检测等结构化手段补）。

### Q8：对比 LangGraph 实现 Agent，你自定义 Agent Runtime 的优势和劣势？为什么不用现成 LangGraph？

**参考答**：
- **优势**：无重依赖、完全可控（协议/重试/并发/压缩每层自己写、可解释）、轻量、真学到 Agent 底层机制。
- **劣势**：功能不全（没有 LangGraph 的图编排、checkpoint、可视化、生态工具），自己写的 bug 自己扛，扩展要自己造轮子。
- **为什么不用**：定位是 Harness Engineering——把机制拆开学，不是追求产品功能面；且 LangGraph 对轻量本地 CLI 是重依赖。
